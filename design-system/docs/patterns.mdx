import { Meta } from '@storybook/addon-docs/blocks';
 
<Meta title="Patterns" />

# Patterns
Patterns are the smallest building blocks in our design system.
They ensure that the design is unified and cohesive.
Patterns include but not limited to sizes, spacing, colours, et cetera.